import { StyleSheet } from 'react-native';

const Fonts = StyleSheet.create({
    Title: {
        color: "#ffffff",
        fontSize: 25,
        fontWeight: '700'

    },
    Text: {
        color: '#ffffff',
        fontSize: 15,
        fontWeight: '600'
    }
});


export default Fonts;